apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

# Bump this and re-apply to roll out a new build.
images:
  - name: beer-darts
    newTag: 0.1.0
